//import { DirectiveUnitTestDirective } from './directive-unit-test.directive';

describe('DirectiveUnitTestDirective', () => {
  it('should create an instance', () => {
    // const directive = new DirectiveUnitTestDirective();
    // expect(directive).toBeTruthy();
  });
});
